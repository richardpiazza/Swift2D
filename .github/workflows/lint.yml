name: Lint

on:
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:

  SwiftFormat:
    runs-on: macos-26

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Lint
        run: swiftformat --lint . --reporter github-actions-log
